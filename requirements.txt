# Core UI
streamlit>=1.40,<2.0

# LangChain core (fixed to 0.1.0)
langchain==0.1.0

# Community extensions (vectorstores, loaders, etc.)
langchain-community>=0.0.30,<0.1.0

# OpenAI client (used by OpenAI LLM and embeddings)
openai>=1.30,<2.0

# Vector store backend (local Chroma DB)
chromadb>=0.5,<0.6

# Tokenizer for OpenAI models
tiktoken>=0.5,<1.0